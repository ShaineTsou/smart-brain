import React from 'react';

const Navigation = () => {
    return(
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='f4 link dim washed-green ph4 pv2 pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;